# 2026-09-01 to 2026-09-04 frozen replay batch

## Result

The latest intel main (`bc86bb3`) completed four additional historical
replays using date-pinned freshness, A-share input, cross-market, and Watch20
inputs:

| source date | signal date | topic count | pipeline failures | degraded |
| --- | --- | ---: | --- | --- |
| 20260901 | 20260902 | 6 | none | moneyflow |
| 20260902 | 20260903 | 6 | none | moneyflow |
| 20260903 | 20260904 | 8 | none | moneyflow |
| 20260904 | 20260907 | 5 | none | moneyflow |

All four runs completed with `charts.failed=[]`. The new pipeline consumed the
correct source-date Watch20 artifact and degraded the unavailable optional
`moneyflow_ths` dataset instead of failing.

## Parity interpretation

The strict comparator reports nine differences for each date. They are stable
and explainable:

- three chart fields: the old manifest reports a successful moneyflow chart,
  while the date-pinned canonical snapshot intentionally excludes the
  quarantined historical moneyflow data and the new pipeline emits a
  placeholder;
- six topic fields: the old manifest points at the prior day's Watch20 run,
  while the replay uses the matching source-date run.

The old comparison root also lacks standalone `manifest.json` and
`weekly_recap.meta.json`. These are old delivery-materialization gaps and are
recorded separately from pipeline behavior.

## Conclusion

The four-date batch provides evidence that the new code is operationally
stable across the historical window. It is not a clean five-day parity gate:
the old expected artifacts are not behaviorally comparable until the input
mapping and quarantined moneyflow provenance are resolved. No new pipeline
exception was observed in this batch.
