# 2026-09-07 frozen production-input replay

## Result

The replay used intel main `9cd238c` and a data-platform snapshot built by
main `8eea5d1`. The A-share input receipt was `106510a3a1ffd48a70082b82`.

- The new pipeline completed without failed charts.
- Dashboard generation completed successfully.
- Missing `moneyflow_ths` was handled as `degraded` with a placeholder, as
  required for an optional dataset.
- Cross-market data was loaded from a date-pinned snapshot.
- The corrected Watch20 artifact was source date `20260907`, signal date
  `20260908`, with six topics.

## Remaining differences

The comparator reported nine differences. They are input and delivery-root
differences, not new pipeline exceptions:

- The old manifest treated the available Watch20 artifact as invalid because
  it pointed at source date `20260904`; the replay used the correct
  `20260907` artifact from the next signal run.
- The old production manifest reports a successful moneyflow chart, while the
  date-pinned core snapshot has no immutable `moneyflow_ths` version covering
  20260907. The new pipeline correctly degrades this optional chart.
- The old comparison root has no standalone `manifest.json` or
  `weekly_recap.meta.json`.

No new chart failed, and the dashboard no longer crashes when optional margin
or moneyflow datasets are absent.

## Source mapping

- Report freshness source:
  `reports/market-intel/a_share_daily/.morning_pipeline_20260907.json`.
- Correct Watch20 source:
  `strategy_outputs/watchlist20/runs/20260908_20260908T003214Z_540f3416/topic_summary.json`.
- Data snapshot datasets: `daily`, `daily_basic`, `adj_factor`, and
  `limit_status`; `hsgt_top10` and `moneyflow_ths` were not available as
  immutable versions covering 20260907.
- The snapshot builder now prefers a version containing the target
  `trade_date=YYYYMMDD` partition, avoiding flat parquet versions that cannot
  be consumed by the report readers.
