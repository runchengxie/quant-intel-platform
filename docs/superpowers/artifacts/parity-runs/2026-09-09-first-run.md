# 首次生产影子 parity 运行记录

日期：2026-09-09  
状态：`needs_explanation`，不能作为切换生产的通过证据

## 运行范围

- source date：`20260908`
- signal date：`20260909`
- old implementation：当前生产 `out/a_share_daily` 输出
- new implementation：公开 `v0.1.4` 的 shadow 输出
- comparator：`tests/parity/run_parity.py`
- 发送：未执行；本次仅比较 JSON artifact

## 结果

比较器以退出码 `1` 结束，摘要如下：

| 项目 | 数量 |
|---|---:|
| mandatory artifacts | 4 |
| old 中缺失 | 1 |
| new 中缺失 | 1 |
| 有差异的文件 | 1 |

manifest 标记为 `unexplained_differences: true`。这表示比较器正确阻止了
“尚未解释的差异被当作 parity 通过”，不表示发送链路发生了发送事故。

## 当前解释

已观察到的主要差异有三类：

1. 两边输出路径属于不同运行根目录；这是环境差异，比较器当前还没有对路径字段做归一化。
2. 旧输出中的跨市场日期与新输出的 source date 不一致，说明本次不是同一个冻结输入快照的严格重放。
3. VIX、TNX、商品和概念等外部市场快照发生数值漂移；这些数据是在不同时间从 live source 获取的，不能直接作为实现逻辑差异。

另有一侧存在 weekly recap 元数据、另一侧存在内部 pipeline marker 的文件集合差异，需在同一快照重跑后判断它们是产品契约差异还是运行批次差异。

## 下一步

1. 冻结并记录同一 source snapshot、数据版本和两边 commit。
2. 在 comparator 中加入明确的环境路径字段归一化规则，保持业务字段严格比较。
3. 用同一快照重跑；对仍存在的每一项差异建立解释或修复，不把 live-data 漂移直接判为代码 parity 失败。
4. 只有在无未解释差异后，才把本次 source/signal date 纳入连续五个交易日的通过计数。

